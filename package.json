{
  "name": "rms-reservation-system",
  "private": true,
  "workspaces": [
    "reservation/backend",
    "reservation/frontend"
  ],
  "scripts": {
    "install:all": "npm install --workspace reservation/backend && npm install --workspace reservation/frontend",
    "dev": "concurrently \"npm run dev --workspace reservation/backend\" \"npm run dev --workspace reservation/frontend\"",
    "start": "npm run start --workspace reservation/backend",
    "build": "npm run build --workspace reservation/frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
